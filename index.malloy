// THE EXPORT SURFACE. Only what this file exports is visible to the dashboards,
// to `dashboard dev`, and to the published site.
import { programs, CREDENTIAL, STATE, CONTROL, MIN_GRADS } from 'degree_roi.malloy'

export { programs, CREDENTIAL, STATE, CONTROL, MIN_GRADS }
