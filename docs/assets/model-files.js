window.__MODEL_FILES__ = {"file:///dashboards/field-compare.malloy":"##! experimental { access_modifiers givens }\n\nimport \"../index.malloy\"\n\n// \"I want to study X - where does it actually pay, and does the credential\n// level change the answer?\" One field, compared across schools and levels.\n\ngiven:\n  # label=\"Field of study\" control=select suggest { source=programs dimension=field }\n  FIELD :: filter<string> is f'Computer Science'\n\n#\" This field, by credential level\n# artifact { title=\"Compare a field\" }\nquery: by_credential is programs -> {\n  where: field ~ $FIELD and state ~ $STATE and control ~ $CONTROL and grads_per_year ~ $MIN_GRADS\n  group_by: credential\n  aggregate:\n    programs_at_level is program_count\n    earnings is med_earnings\n    debt is med_debt\n    ratio is avg_debt_ratio\n    grads is total_grads\n  order_by: earnings desc\n}\n\n#\" This field, by school type\nquery: by_control is programs -> {\n  where: field ~ $FIELD and credential ~ $CREDENTIAL and state ~ $STATE and grads_per_year ~ $MIN_GRADS\n  group_by: control\n  aggregate:\n    programs_of_type is program_count\n    earnings is med_earnings\n    debt is med_debt\n    ratio is avg_debt_ratio\n  order_by: earnings desc\n}\n\n#\" Where this field pays best\nquery: top_schools is programs -> {\n  where: field ~ $FIELD and credential ~ $CREDENTIAL and state ~ $STATE\n     and control ~ $CONTROL and grads_per_year ~ $MIN_GRADS\n  group_by: school, state, control\n  aggregate:\n    earnings is med_earnings\n    debt is med_debt\n    ratio is avg_debt_ratio\n    grads is total_grads\n  order_by: earnings desc\n  limit: 50\n}\n\n#\" Which states have the most programs in this field\nquery: by_state is programs -> {\n  where: field ~ $FIELD and credential ~ $CREDENTIAL and control ~ $CONTROL and grads_per_year ~ $MIN_GRADS\n  group_by: state\n  aggregate:\n    programs_in_state is program_count\n    earnings is med_earnings\n    ratio is avg_debt_ratio\n  order_by: programs_in_state desc\n  limit: 25\n}\n","file:///dashboards/school-profile.malloy":"##! experimental { access_modifiers givens }\n\nimport \"../index.malloy\"\n\n// \"I'm looking at this school - which of its programs actually pay off?\"\n// Every reported program at one institution, ranked.\n\ngiven:\n  # label=\"School\" control=select suggest { source=programs dimension=school }\n  SCHOOL :: filter<string> is f'North Carolina State University at Raleigh'\n\n#\" Headline for this school\n# artifact { title=\"School profile\" }\nquery: school_headline is programs -> {\n  where: school ~ $SCHOOL and credential ~ $CREDENTIAL\n  aggregate:\n    programs_reported is program_count\n    earnings is med_earnings\n    debt is med_debt\n    ratio is avg_debt_ratio\n    grads is total_grads\n}\n\n#\" Every reported program here, best value first\nquery: school_programs is programs -> {\n  where: school ~ $SCHOOL and credential ~ $CREDENTIAL and grads_per_year ~ $MIN_GRADS\n  group_by: field, credential\n  aggregate:\n    earnings is med_earnings\n    debt is med_debt\n    ratio is avg_debt_ratio\n    grads is total_grads\n  order_by: ratio asc\n  limit: 200\n}\n\n#\" What this school offers, by credential level\nquery: school_by_level is programs -> {\n  where: school ~ $SCHOOL\n  group_by: credential\n  aggregate:\n    programs_at_level is program_count\n    earnings is med_earnings\n    ratio is avg_debt_ratio\n  order_by: programs_at_level desc\n}\n","file:///dashboards/value-ranking.malloy":"##! experimental { access_modifiers givens }\n\nimport \"../index.malloy\"\n\n// The headline dashboard: rank every program in the country by what graduates\n// owe relative to what they earn. This is the question the official College\n// Scorecard cannot answer - it is built to look up one school at a time.\n\ngiven:\n  # label=\"Field of study\" suggest { source=programs dimension=field }\n  FIELD :: filter<string> is f''\n\n#\" Headline numbers for the current selection\n# artifact { title=\"Value ranking\" }\nquery: headline is programs -> {\n  where: credential ~ $CREDENTIAL and state ~ $STATE and control ~ $CONTROL\n     and field ~ $FIELD and grads_per_year ~ $MIN_GRADS\n  aggregate:\n    programs_shown is program_count\n    typical_earnings is med_earnings\n    typical_debt is med_debt\n    typical_ratio is avg_debt_ratio\n    graduates is total_grads\n    typical_payment is med_payment\n    beat_hs_num is above_hs_num\n    beat_hs_den is above_hs_den\n    typical_earnings_5yr is med_earn_5yr\n    typical_ratio_5yr is avg_debt_ratio_5yr\n}\n\n#\" Best value: lowest debt relative to earnings\nquery: best_value is programs -> {\n  where: credential ~ $CREDENTIAL and state ~ $STATE and control ~ $CONTROL\n     and field ~ $FIELD and grads_per_year ~ $MIN_GRADS and debt_median > 0\n  group_by: school, field, credential, state, control\n  aggregate:\n    earnings is med_earnings\n    debt is med_debt\n    ratio is avg_debt_ratio\n    grads is total_grads\n    payment is med_payment\n    earnings_5yr is med_earn_5yr\n    ratio5 is avg_debt_ratio_5yr\n  order_by: ratio asc\n  limit: 60\n}\n\n// RANKED ON THE FIVE-YEAR FIGURE, deliberately. Ranking the heaviest-debt list\n// on year-one pay put Communication Disorders at Commonwealth University of PA\n// at rank 1 in the country on a $4,880 median - a figure that mostly measures\n// graduates who were still in a master's programme. Those graduates earn about\n// $70,300 by year five. Naming a real institution as the worst in the country on\n// an artefact of measurement timing is not a disclosure problem to be footnoted;\n// it is a ranking problem. Programs without a five-year figure (about 12%) are\n// excluded here rather than ranked on a basis that misrepresents them.\n#\" Heaviest debt load relative to FIFTH-year earnings\nquery: worst_value is programs -> {\n  where: credential ~ $CREDENTIAL and state ~ $STATE and control ~ $CONTROL\n     and field ~ $FIELD and grads_per_year ~ $MIN_GRADS and debt_median > 0\n     and earn_5yr != null\n  group_by: school, field, credential, state, control\n  aggregate:\n    earnings is med_earnings\n    debt is med_debt\n    ratio is avg_debt_ratio\n    grads is total_grads\n    payment is med_payment\n    earnings_5yr is med_earn_5yr\n    ratio5 is avg_debt_ratio_5yr\n  order_by: ratio5 desc\n  limit: 60\n}\n\n#\" How the selection splits across the plain-language debt bands\nquery: verdict_split is programs -> {\n  where: credential ~ $CREDENTIAL and state ~ $STATE and control ~ $CONTROL\n     and field ~ $FIELD and grads_per_year ~ $MIN_GRADS\n  group_by: debt_verdict\n  aggregate:\n    programs_in_band is program_count\n    graduates_in_band is total_grads\n  order_by: programs_in_band desc\n}\n\n// ---------------------------------------------------------------------------\n// Chart feeds. These aggregate to FIELD level on purpose: \"is nursing worth it\"\n// is the question people actually have, and 22,000 individual program dots is\n// both unreadable and slow to render.\n// ---------------------------------------------------------------------------\n\n#\" Every field of study as one point: what it pays vs what it costs\nquery: field_scatter is programs -> {\n  where: credential ~ $CREDENTIAL and state ~ $STATE and control ~ $CONTROL\n     and field ~ $FIELD and grads_per_year ~ $MIN_GRADS and debt_median > 0\n  group_by: field\n  aggregate:\n    earnings is med_earnings\n    debt is med_debt\n    ratio is avg_debt_ratio\n    grads is total_grads\n    programs_in_field is program_count\n  order_by: grads desc\n  limit: 300\n}\n\n#\" The shape of the distribution: how many programs sit in each debt band\nquery: ratio_distribution is programs -> {\n  where: credential ~ $CREDENTIAL and state ~ $STATE and control ~ $CONTROL\n     and field ~ $FIELD and grads_per_year ~ $MIN_GRADS and debt_median > 0\n  group_by: ratio_bin\n  aggregate: programs_in_bin is program_count\n  order_by: ratio_bin asc\n}\n\n#\" The biggest fields by graduate volume - where most people actually are\nquery: biggest_fields is programs -> {\n  where: credential ~ $CREDENTIAL and state ~ $STATE and control ~ $CONTROL\n     and field ~ $FIELD and grads_per_year ~ $MIN_GRADS and debt_median > 0\n  group_by: field\n  aggregate:\n    earnings is med_earnings\n    debt is med_debt\n    ratio is avg_debt_ratio\n    grads is total_grads\n  order_by: grads desc\n  limit: 18\n}\n\n#\" Did the credential beat not having one? Biggest fields by share earning above a high-school graduate\nquery: beat_hs_by_field is programs -> {\n  where: credential ~ $CREDENTIAL and state ~ $STATE and control ~ $CONTROL\n     and field ~ $FIELD and grads_per_year ~ $MIN_GRADS and above_hs_denom > 0\n  group_by: field\n  aggregate:\n    share_num is above_hs_num\n    share_den is above_hs_den\n    earnings is med_earnings\n    payment is med_payment\n    grads is total_grads\n  order_by: grads desc\n  limit: 16\n}\n\n#\" Year one is a first job, not a career: how pay moves between year 1 and year 5\nquery: career_climb is programs -> {\n  where: credential ~ $CREDENTIAL and state ~ $STATE and control ~ $CONTROL\n     and field ~ $FIELD and grads_per_year ~ $MIN_GRADS\n     and earn_5yr != null and earn_1yr != null\n  group_by: field\n  aggregate:\n    year1 is med_earnings\n    year5 is med_earn_5yr\n    grads is total_grads\n    programs_in_field is program_count\n  order_by: grads desc\n  limit: 16\n}\n","file:///dashboards/zz-about.malloy":"##! experimental { access_modifiers givens }\n\nimport \"../index.malloy\"\n\n// About / methodology. The coverage numbers are QUERIED, not hand-written, so\n// this page cannot drift from the data it is describing.\n\n#\" What is actually in here\n# artifact { title=\"About the data\" }\nquery: coverage is programs -> {\n  aggregate:\n    total_programs is program_count\n    graduates is total_grads\n}\n\n// Distinct counts are pipelines (group, then count the groups) rather than a\n// distinct-count aggregate, which is not a thing you can call on a dimension.\n#\" Distinct schools represented\nquery: school_count is programs -> { group_by: school } -> { aggregate: how_many is count() }\n#\" Distinct fields of study represented\nquery: field_count is programs -> { group_by: field } -> { aggregate: how_many is count() }\n#\" Distinct states represented\nquery: state_count is programs -> { group_by: state } -> { aggregate: how_many is count() }\n\n#\" Coverage by credential level\nquery: coverage_by_level is programs -> {\n  group_by: credential\n  aggregate:\n    programs_at_level is program_count\n    earnings is med_earnings\n    debt is med_debt\n    ratio is avg_debt_ratio\n  order_by: programs_at_level desc\n}\n","file:///degree_roi.malloy":"##! experimental { access_modifiers givens }\n\nimport \"storage.malloy\"\n\n// ---------------------------------------------------------------------------\n// Controls shared across dashboards. Each dashboard adds its own on top.\n// ---------------------------------------------------------------------------\ngiven:\n  # label=\"Credential\" control=select suggest { source=programs dimension=credential }\n  CREDENTIAL :: filter<string> is f'Bachelor\\'s Degree'\n  # label=\"State\" control=select suggest { source=programs dimension=state }\n  STATE :: filter<string> is f''\n  # label=\"School type\" control=select suggest { source=programs dimension=control }\n  CONTROL :: filter<string> is f''\n  // Defaults to 10/yr rather than 0. A programme graduating a handful of people\n  // has a median that swings wildly, and with no floor the \"heaviest debt\" list\n  // led with exactly those. Clear the control to see everything.\n  # label=\"Minimum graduates per year\"\n  MIN_GRADS :: filter<number> is f'>= 10'\n\n// ---------------------------------------------------------------------------\n// The model.\n//\n// One row = one PROGRAM: a school x 4-digit CIP field x credential level.\n// Every row already carries BOTH a median-earnings and a median-debt figure -\n// build-data.sh drops the rest, because a program missing either side cannot be\n// ranked on value and a half-populated row would quietly skew every ranking.\n// ---------------------------------------------------------------------------\n#\" US college programs: what graduates earn, and what they borrowed to get there\nsource: programs is programs_table\ninclude {\n  public:\n    #\" Institution name\n    # drill { to=[\"school-profile\"] }\n    school,\n    #\" Field of study (4-digit CIP)\n    # drill { to=[\"field-compare\"] }\n    field,\n    #\" Credential level awarded\n    credential,\n    #\" Two-letter state code\n    state,\n    #\" Public / Private nonprofit / Private for-profit\n    control,\n    *\n}\nextend {\n\n  dimension:\n    #\" Typical graduates per year (average of the two reported award years)\n    grads_per_year is (coalesce(grads_yr1, 0) + coalesce(grads_yr2, 0)) / 2\n\n    // ---- The headline metric -------------------------------------------\n    // Debt as a multiple of one year's earnings. This is deliberately the\n    // gainful-employment-style ratio rather than an invented composite score:\n    // it is the measure regulators actually use, it needs no weighting choices\n    // we would have to defend, and it is directly interpretable.\n    // LOWER IS BETTER. 1.0 means a graduate owes exactly one year's pay.\n    #\" Debt as a multiple of first-year earnings (lower is better)\n    debt_ratio is debt_median / earn_1yr\n\n    #\" First-year earnings minus total borrowed (blunt, but the question people ask)\n    net_first_year is earn_1yr - debt_median\n\n    // A ratio is abstract. A monthly payment is what actually lands in someone's\n    // bank account, so it is the number this site leads with. This is the\n    // DEPARTMENT'S own 10-year standard-repayment figure, not one we derived -\n    // which keeps \"we add nothing but arithmetic\" literally true.\n    #\" Monthly payment on that debt, 10-year standard repayment (the Department's own figure)\n    monthly_cost is monthly_payment\n\n    #\" Loan payments as a share of gross monthly pay\n    payment_share is (monthly_payment * 12) / earn_1yr\n\n    // The question a debt-to-pay ratio cannot answer: did the credential beat\n    // not having one? The Department's own benchmark, 5 years after finishing.\n    #\" Share earning more than a typical high-school graduate, 5 years out\n    beat_hs is above_hs_n / above_hs_denom\n\n    #\" Earnings growth from year 1 to year 5, where both are reported\n    earnings_growth_5yr is earn_5yr - earn_1yr\n\n    // Year 1 is a FIRST JOB, not a career. Median pay rises 59% between year 1\n    // and year 5, and the rise is wildly uneven: fields that normally feed into\n    // graduate school (speech pathology, biology, neuroscience) look catastrophic\n    // at year 1 because many of those graduates are still studying. Carrying the\n    // 5-year figure is the only career-length signal this dataset has at this\n    // grain, and without it the site defames exactly those fields.\n    #\" Debt as a multiple of FIFTH-year earnings (lower is better)\n    debt_ratio_5yr is debt_median / earn_5yr\n\n    // A plain-language band. The cut points are the ones the Department's own\n    // gainful-employment framing uses as a rough guide, exposed here as words\n    // rather than a number so a reader does not have to interpret a ratio.\n    // Binned ratio for the distribution chart. 0.25 buckets, capped at 3.0 so\n    // the long thin tail collapses into one readable \"3+\" bar instead of\n    // stretching the axis to the single worst program in the country.\n    #\" Debt-to-earnings ratio, bucketed for the distribution chart\n    ratio_bin is\n      pick 3.0 when debt_ratio >= 3.0\n      else floor(debt_ratio * 4) / 4\n\n    #\" Plain-language read on the debt-to-earnings ratio\n    debt_verdict is\n      pick 'No debt reported' when debt_median = 0\n      pick 'Light - under half a year of pay' when debt_ratio < 0.5\n      pick 'Moderate - under one year of pay'  when debt_ratio < 1.0\n      pick 'Heavy - one to two years of pay'   when debt_ratio < 2.0\n      else 'Very heavy - over two years of pay'\n\n  measure:\n    #\" Number of programs matching\n    program_count is count()\n    #\" Median first-year earnings across the matching programs\n    med_earnings is earn_1yr.avg()\n    #\" Median debt across the matching programs\n    med_debt is debt_median.avg()\n    #\" Average debt-to-earnings ratio across the matching programs\n    avg_debt_ratio is debt_ratio.avg()\n    #\" Total graduates per year across the matching programs\n    total_grads is grads_per_year.sum()\n    #\" Typical monthly loan payment across the matching programs\n    med_payment is monthly_payment.avg()\n    #\" Median pay five years after finishing\n    med_earn_5yr is earn_5yr.avg()\n    #\" Debt against fifth-year pay\n    avg_debt_ratio_5yr is debt_ratio_5yr.avg()\n\n    // Ratios are aggregated as SEPARATE numerator and denominator sums, then\n    // divided at the point of display. Taking .avg() of a ratio-of-columns\n    // dimension gave 20116 and 50546.75 where DuckDB says 0.0806 and 0.8568 -\n    // caught 2026-08-04 by checking the rendered figures against the parquet.\n    // Summing the parts is also the more defensible statistic: it weights by\n    // graduates, so it describes PEOPLE rather than treating a 12-graduate\n    // program as equal to a 1,200-graduate one.\n    #\" Graduates out-earning a high-school graduate (numerator)\n    above_hs_num is above_hs_n.sum()\n    #\" Graduates measured for that comparison (denominator)\n    above_hs_den is above_hs_denom.sum()\n}\n","file:///index.malloy":"// THE EXPORT SURFACE. Only what this file exports is visible to the dashboards,\n// to `dashboard dev`, and to the published site.\nimport { programs, CREDENTIAL, STATE, CONTROL, MIN_GRADS } from 'degree_roi.malloy'\n\nexport { programs, CREDENTIAL, STATE, CONTROL, MIN_GRADS }\n","file:///storage.malloy":"// Where the data lives. Docs-local hosting: the parquet is committed inside\n// docs/ and served by GitHub Pages, and the BUNDLED SITE'S ROOT *IS* docs/ -\n// so the path here must be relative to the site root ('programs.parquet'),\n// NOT to the project root ('docs/programs.parquet').\n//\n// Getting this wrong fails SILENTLY and is worth knowing about: the page still\n// renders, every control works, and every tile just reads 0 / \"no rows\",\n// because the fetch 404s and nothing surfaces the error. Verified 2026-08-04 by\n// watching the HTTP log for a programs.parquet request that never came.\nsource: programs_table is duckdb.table('programs.parquet') extend {}\n"};
window.__TABLE_FILES__ = {"programs.parquet":"./programs.parquet"};
window.__DUCKDB_BASE__ = "./duckdb/";
